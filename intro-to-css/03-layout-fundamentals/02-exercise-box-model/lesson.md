---
title: "Exercise: Space a Seed Packet"
slug: exercise-box-model
order: 2
language: css
lesson_type: coding
runtime: srcdoc
summary: Use padding, a border, and a margin to turn a plain seed listing into a packet card.
seo_title: CSS Box Model Exercise | Space a Seed Packet
seo_description: Practice CSS padding, borders, and margins by spacing a Riverside Seed Swap packet card.
seo_keywords:
  - CSS box model exercise
  - CSS padding
  - CSS border
  - CSS margin
  - card spacing
hints:
  - Put each declaration inside the `.packet` rule.
  - The margin belongs outside the border.
---

# Space a seed packet

The demo showed one completed Riverside Seed Swap packet. This listing has the same content but no packet spacing yet.

Add the three box-model layers in `style.css`. **Checkpoint:** the cream card should gain an inner buffer, a green edge, and space above and below it.

## Your Tasks

1. Add `padding: 20px;` to `.packet` so its text sits away from the edge.
2. Add `border: 4px solid #3c6e47;` to `.packet` to draw the packet outline.
3. Add `margin: 24px 0;` to `.packet` to separate it from nearby content.

When all three checks pass, the listing reads as a distinct seed packet.
