---
title: Color Palette Exercise
slug: exercise-color-palette
order: 2
language: html
summary: Build a readable three-color CSS palette for a Lantern Lane Night Market ticket.
seo_title: CSS Color Palette Exercise | Visual Style
seo_description: Practice CSS color and background-color declarations by styling a readable night-market ticket.
seo_keywords:
  - CSS color exercise
  - background-color
  - hex colors
  - CSS styling
lesson_type: coding
hints:
  - Use `background-color` for the ticket surface and `color` for text.
  - Keep each declaration inside the selector named in the task.
---

# Color Palette Exercise

The preceding demo used a dark surface, bright heading, and focused accent. Now give this small ticket the same clear hierarchy.

Run the preview after each change. A completed ticket has a navy surface, a cream title, and a coral entry badge.

## Your Tasks

1. Set `.ticket` to a `#172554` background color.
2. Set the `h1` text color to `#fff7ed`.
3. Set `.entry` to a `#fb7185` background color and `#172554` text color.
