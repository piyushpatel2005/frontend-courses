---
title: Background Layer Demo
slug: background-layer-demo
order: 3
language: html
summary: See how a CSS linear gradient layers color over a night-market hero panel.
seo_title: CSS Background Gradient Demo | Visual Style
seo_description: Inspect a CSS linear-gradient and background color working together in a night-market hero panel.
seo_keywords:
  - CSS backgrounds
  - linear gradient
  - background image
  - CSS demo
lesson_type: coding
hints:
  - "`background-image` can hold a gradient without an image file."
---

# Background Layer Demo

The market needs a welcome panel that feels brighter than a plain block of color. This demo layers a diagonal gradient over a fallback `background-color`.

In `.hero`, `background-image: linear-gradient(135deg, #7c3aed, #ec4899);` creates a color blend. The `135deg` value sets the direction; the two colors set the stops.

## Visible checkpoint

Run the preview. The violet-to-pink panel should make the white event title easy to read.

## Try it safely

Change `135deg` to `90deg`, run the preview, and observe the new gradient direction. Restore `135deg` before moving on.

Next, create the background layer yourself in **Background Layer Exercise**.
